step_lambda_aws_region   = "ap-southeast-1"
step_lambda_project_name = "step-lambda"
step_lambda_environment  = "production"

# Domain you control; MX must point to inbound-smtp.<region>.amazonaws.com for SES inbound
step_lambda_ses_domain    = "alerts.example.com"
step_lambda_ses_recipient = "triage@alerts.example.com"

step_lambda_bedrock_model_id = "global.anthropic.claude-haiku-4-5-20251001-v1:0"

# Comma-separated From allowlist; empty = allow all
step_lambda_filter_from_emails = "alerts@example.com,pager@example.com"

# Comma-separated Slack member IDs (U…) / usergroup IDs (S…) / @handles to notify
step_lambda_slack_notify_handles = "U012ABCDEF,S012GHIJKL"

step_lambda_secret_values = {
  JIRA_BASE_URL            = "https://your-org.atlassian.net"
  JIRA_EMAIL               = "bot@example.com"
  JIRA_API_TOKEN           = "replace-me"
  JIRA_PROJECT_KEY         = "OPS"
  JIRA_ASSIGNEE_ACCOUNT_ID = "712020:xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
  SLACK_BOT_TOKEN          = "xoxb-replace-me"
  SLACK_CHANNEL            = "#ops-alerts"
}
